import { AuthCard } from "../_components/auth-card";

export const metadata = {
  title: "Create account | Discovery Platform",
};

export default function SignupPage() {
  return <AuthCard mode="signup" />;
}
